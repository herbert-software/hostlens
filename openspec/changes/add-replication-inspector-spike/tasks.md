## 1. 录制 lane:master+replica 拓扑

- [x] 1.1 在 `tests/inspectors/compose/docker-compose.yml` 加 `redis-repl-master`(`redis:7.4.2-alpine`,与既有 redis 同 pin)+ `redis-repl-replica`(`replicaof redis-repl-master 6379`),两者 pin `--repl-ping-replica-period 10` + 大 `--repl-timeout`(如 3600,使 `DEBUG SLEEP` 期间链路不因超时判 down);`redis-repl-master` 额外加 `--enable-debug-command yes`(redis 7.x 默认禁 DEBUG,不开则 `DEBUG SLEEP` 被拒,**仅录制 lane**);各带 `redis-cli PING` healthcheck;**禁固定 sleep**。验收:`docker compose config` 解析通过;手动 `up` 后 `redis-cli -h replica INFO replication` 显示 `role:slave` + `master_link_status:up`,且 `redis-cli -h master DEBUG SLEEP 0` 不被拒。
- [x] 1.2 在 `_compose_record.py` 复用现有 `wait_healthy()`,补一个 `wait_until(predicate, timeout)` poll helper(poll 一个状态条件,非等时长),供录制器等 `master_link_status==down` / `master_last_io_seconds_ago>=N`。验收:helper 单测——predicate 立即真则 0 轮返回;超时抛错不静默。

## 2. redis.replication_lag manifest

- [x] 2.1 写 `src/hostlens/inspectors/builtin/redis/replication_lag.yaml`:`targets:[local,ssh]`、`requires_capabilities:[shell]`(静态)、`requires_binaries:[redis-cli]`、`secrets:[HOSTLENS_REDIS_PASSWORD]`、参数仅 `host`/`port`(带 `pattern`/范围)+ `warn_seconds`(默认 15)/`critical_seconds`(默认 30)(均 > `repl-ping-replica-period` 10s 防健康空闲副本误报;参数名**真正回避** `replica`/`lag`/`primary` 等子串,D-6;不靠"守卫是精确 key 匹配"兜底)。`description` **必须声明** `lag_seconds` 语义类 = `link_freshness`(`master_last_io_seconds_ago`,非 apply 滞后,D-1/D-3)。collector 跑 `INFO replication`、在命令内派生归一三元组 `replication_configured`/`link_healthy`/`lag_seconds`。**fail-loud 须按 role 上下文**:`role` 字段**始终必需**(缺失/redis-cli 非零退出→exit 1+空 stdout→exception);**replica-only 字段**(`master_link_status`/`master_last_io_seconds_ago`)**仅当 `role==slave` 时必需**——`role!=slave`(standalone/primary)走未配置路径输出 `replication_configured=false`/`link_healthy=false`/`lag_seconds=null`(**不 exit 1**,见 D-4),`role==slave` 而 replica-only 字段缺失才 exit 1。把 `master_last_io_seconds_ago:-1`(同步/链路重建哨兵)归一成 `lag_seconds=null`(不输出 -1)。验收:`load_manifest` 通过;`-t 5` < `timeout_seconds`;secret 经 `REDISCLI_AUTH` remap、**不进 argv**、空密码走 no-auth 分支(逐字对齐 `redis.memory_usage`)。
- [x] 2.2 写 findings(三态 by-finding,D-4):未配置→无 finding;`replication_configured && !link_healthy`→critical「replication link down」;`link_healthy && lag_seconds>=critical_seconds`→critical;`link_healthy && lag_seconds>=warn_seconds`→warning。`lag_seconds` null-guard。验收:DSL 只比三元组标量,无 DB 专有原始字段;`output_schema` 含三元组且 `lag_seconds` 类型 `[integer,"null"]`。
- [x] 2.3 「非 root 用户跑通」验收:以非 root 在本地对一个 standalone redis(无复制)跑 `hostlens inspect redis.replication_lag`→status=ok、`replication_configured=false`、findings 空(D-4 未配置态)。

## 3. 双轨 fixture + 单探针 snapshot 测试

- [x] 3.1 写 `tests/inspectors/_record_redis_replication_lag.py`,产 5 个录制 fixture:`healthy`(link up、刚 ping/主动写后录制使 `master_last_io_seconds_ago` ~0、默认阈值 15/30 无 finding)、`finding_trigger`(健康副本 + `warn_seconds=0` 只证接线)、`link_down`(**停 master 容器**=TCP 断开,链路秒级判 down、**不依赖 `repl-timeout`** + poll 到 `master_link_status==down` 后冻结,`link_healthy=false`)、`link_stale`(master 异步 `DEBUG SLEEP ~35`(> critical_seconds 30、<< repl-timeout) + 经另一连接 poll 到 `master_link_status==up` 且 `master_last_io_seconds_ago>=30` 后冻结真实值,`link_healthy=true`、lag 高)、`conn_refused`(连关闭端口、fail-loud)。两个 semantic-abnormal **语义必须不同**:录制时断言 `link_down` 的 `master_link_status==down`、`link_stale` 的 `==up`(D-5)。`requires_unmet` 经无二进制 stub 断言(无录制文件)。**禁固定 sleep**。验收:录制器头注释写明重录步骤;readiness 全走 `wait_until`。
- [x] 3.2 写 `tests/inspectors/test_redis_replication_lag.py`,对每个 fixture 走 ReplayTarget 回放 + snapshot:断言 `status=="requires_unmet"`(无二进制 stub)、`status=="ok"`(healthy)、`status=="exception"`(conn_refused)三态覆盖齐;`link_down` 在**默认**阈值触发 critical「link down」;`link_stale` 在**默认**阈值按真实 `lag_seconds` 触发(link up 分支)。验收:CI 只跑回放(不起容器);snapshot 容忍尾换行(`.rstrip("\n")`)。
- [x] 3.3 「密钥不进日志/报告」脱敏验收:`link_down`/`link_stale` 录制注入一个特殊字符密码(复用 `"p w*d"` 量级),断言冻结 fixture 的 stdout/stderr/command 文本**不含**该明文值;fixture 无 per-command `env` 字段。

## 4. 独立 replication crosscheck

- [x] 4.1 写 `tests/inspectors/test_replication_contract_crosscheck.py`,**复跑**继承的单实例契约项(D-6a):注入 payload 被 schema `pattern` 在命令渲染前拒绝;secret `HOSTLENS_REDIS_PASSWORD`→`REDISCLI_AUTH` remap 且无 `-a ` argv 明文;无 `target.type`/`$TARGET` 分叉;`-t 5`<`timeout_seconds`;输出为聚合标量。验收:全部对 `redis.replication_lag` 单条 manifest 断言通过。
- [x] 4.2 在同文件加复制专属断言(D-6b):`output_schema` 含 `replication_configured`/`link_healthy`/`lag_seconds` 三元组;`description` 声明了 lag 语义类(`link_freshness`);findings 覆盖三态(未配置无 finding / 链路断 critical / 陈旧滞后阈值)；存在 `link_down` 与 `link_stale` 两个**语义不同**的 semantic-abnormal fixture 且在默认阈值触发;参数名不含 `replica`/`lag`/`primary` 等子串。验收:测试红当三元组缺字段、语义类未声明或 fixture 缺一。
- [x] 4.3 核查无回归(D-6 rglob 消费面,**须主动通过非仅"计数不误红"**):① `test_service_contract_crosscheck.py` 的 `_ALL_SERVICE_MANIFESTS`(11)/`_SECRET_SERVICE_MANIFESTS`(6)是显式 dict——确认未加入、计数仍 11/6、`test_no_multi_instance_params` 不红;② `test_builtin_capability_gate.py`(`rglob` 全量)——确认新 manifest **通过** `test_builtin_requires_only_static_capabilities`(只声明 `shell`)、宽松计数下界 `>=12` 仍满足、binary/secret gate 显式 list 不自动纳入;③ `test_builtin_inspectors.py`——确认三个 `*_all_register_with_no_errors` 因新 manifest **干净加载**(`result.errors==[]`)而仍绿、wave cohort 子集断言不受多一个 builtin 影响。验收:`pytest tests/inspectors -q` 全绿(0 error),逐一点名上述三处。

## 5. 裁定 + 固化 + 收尾

- [x] 5.1 把 D-8 裁定(**核心:lag 语义异构 + wave-2c hand-off 更正** / redis 已交付 freshness / mysql 高置信可沿用 + 映射 + NULL/版本列名坑 / postgres 形态分叉待录制验证否则推 wave-3 + idle guard 坑)固化进 spec 的「复制 spike 必须裁定契约可沿用性并记录 lag 语义异构发现」需求与 design,确保 `add-replication-lag-inspectors`(wave-2c)可援引。验收:spec 含三条裁定场景(语义异构核心发现 / 同形可沿用带坑 / 分叉待验证带坑)。
- [x] 5.2 `openspec-cn validate add-replication-inspector-spike --strict` 通过;在临时副本实测 `openspec-cn archive` rebuild 不报中文标题/RENAMED 类错误(归档前预检)。验收:两命令均 0 退出。
- [x] 5.3 全量 `pytest tests/inspectors -q` + `mypy --strict src/hostlens` + lint 绿;PR 前按 §5.3 自评是否跑对抗性 review(本变更含 src manifest + 新测试 + 新 spec,倾向跑)。验收:CI 三检查(lint+type+test py3.11/py3.12)预期绿。
