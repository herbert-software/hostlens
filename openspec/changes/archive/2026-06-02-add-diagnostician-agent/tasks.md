## 1. 数据模型与 schema

- [x] 1.1 `agent/diagnostician.py` 新增 frozen `DiagnosticianResult`（narrative / findings(带 id，canonical) / hypotheses / status:ReportStatus / planner_result / `diagnostician_loop: LoopResult | None`）；验收：构造测试断言字段集 + frozen + `diagnostician_loop` 可为 None；`model_dump_json` 可序列化
- [x] 1.2 `tools/schemas/correlate_findings.py`：`CorrelateFindingsInput`（description / confidence∈{low,medium,high} / `supporting_findings:list[str]`(**序号标签**如 F1/F2) / suggested_actions:list[str]，extra="forbid"）+ `CorrelateFindingsOutput`（ack：`accepted:bool` + 可选 echo 接受的**标签**；**禁止**回传真 `Finding.id`）；验收：input 字段与 `RootCauseHypothesis` 对齐、supporting_findings 为标签、output 不含真 id 的单测
- [x] 1.3 `tools/schemas/request_more_inspection.py`：`RequestMoreInspectionInput`（inspector_name + 可选 parameters；**无** target_name，target 由闭包固定）+ `RequestMoreInspectionOutput`（含 `status`(ok/timeout/target_unreachable/requires_unmet/exception) + findings(**带 id + 序号标签**)）；验收：output schema 含 status + id + label 字段的单测

## 2. finding-store 与装配层 id 盖章

- [x] 2.1 实现 per-run `FindingStore`（**唯一键=序号标签**，`label(F1..) → 盖章 Finding(含真 id)`；seed / append(分配新唯一标签) / resolve_label→real_id / contains(label)；id 可非唯一、多标签可映射同 id）；验收：(a) seed+append+resolve 单测，确认 per-run 实例非 module-global；(b) **同 (inspector,version,message) 不同 severity 的两 finding → 两唯一标签 → resolve 到同一真 id 不互相覆盖** 的碰撞单测
- [x] 2.2 实现装配层 id 盖章 helper：从 `loop_result.tool_invocations` 按 `(inspector_name→findings)` 分组、用 `InspectorRegistry.get(name)` 返回的 `InspectorManifest.version` 反查、`compute_finding_id` 盖章（禁止用已 flatten 的 `PlannerResult.findings` 做分组源）；registry 与 Planner 跑时同一实例；验收：含两 inspector invocation 的 LoopResult → 每 finding 得稳定 id + 正确 name/version
- [x] 2.3 盖章 inspector 缺失 fail-loud：`InspectorRegistry.get(name)` 抛 `inspector_not_found` 时 helper 必须冒泡（不静默跳过）；验收：mock 卸载某 inspector → 盖章抛异常 → CLI 边界 `internal:...` exit 2 的单测
- [x] 2.4 验收：盖章不触碰 wire —— 回放既有 incident/demo/planner cassette 全部命中（`pytest tests/.../test_*replay*`），`run_inspector` tool_result 字节不变

## 3. 诊断师工具与受限注册表

- [x] 3.1 `correlate_findings` ToolSpec（`surfaces={"agent"}` / `side_effects="none"` / `sensitive_output=False`）：handler 闭包持 `FindingStore`，用它 resolve supporting_findings 序号标签**仅为命中校验**（悬空标签 → 结构化 error envelope，不 crash、不静默接受）；**handler 不记录真 id**（output 仅 ack/echo 标签；真 id 由编排层在 harvest 时 resolve，见 4.3）；验收：命中→accepted、悬空→error envelope 两个单测
- [x] 3.2 `request_more_inspection` ToolSpec（`surfaces={"agent"}` / `side_effects="read"` / `sensitive_output=True`）：新写 handler 复刻 `run_inspector_handler` 编排 —— ① `ctx.inspector_registry.get(name)` 拿 manifest(not_found→ToolError) → ② `ctx.target_registry.get(闭包 target_name)` 拿 ExecutionTarget 对象(unknown→ToolError) → ③ **clock 注入**(沿用 register_default_tools(clock=) 先例) → ④ `InspectorRunner.run(manifest, target, parameters=dict(args.parameters) if args.parameters else None, allow_privileged=False, cancel=ctx.cancel)`(**parameters 透传勿漏**，与 run_inspector_handler 一致) → ⑤ **version 直接用 `InspectorResult.version`**(禁止反查 registry) 调 compute_finding_id 盖 id → ⑥ FindingStore 分配新唯一标签 + append → 返回带 status/id/label findings；验收：(a) 补查后新 finding 进 store 且后续可 resolve 的单测；(b) inspector 非 ok 状态时 output.status 如实暴露的单测；(c) unknown inspector → ToolError 单测；(d) unknown target(闭包 target_name 不在 registry) → ToolError 单测；(e) 带 parameters 的补查透传到 InspectorRunner.run 的单测
- [x] 3.3 `tools/diagnostician_tools.py` 的 `register_diagnostician_tools(registry, *, finding_store, target_name, clock=None)`（clock 镜像 register_default_tools 透传给 request_more_inspection 的 InspectorRunner；--intent 传 None）：注册 correlate_findings + request_more_inspection + 复用既有 list_inspectors，**禁止** list_targets；验收：断言注册表含三件套且不含 list_targets

## 4. DiagnosticianAgent 与 prompt

- [x] 4.1 `agent/prompts/diagnostician.md`：诊断师系统提示（角色=跨信号关联+根因；调度纪律：基于已给带标签 findings 推理、用序号标签引用、证据不足才 request_more_inspection、**绝不在发出 request_more_inspection 的同一 turn 引用其结果标签——必须等下一轮看到 tool_result 的标签再 correlate**、每条假设调一次 correlate_findings、收尾 narrative）+ `{tool_overview}` 占位；与 planner.md 同构。**验收（诚实化）**：(a) 单测**主动构造**同 turn 前向引用序列（authored FakeBackend 在一个 turn 同时发 request_more_inspection + 引用其未返回标签的 correlate_findings），断言 handler→error envelope→loop 喂回→若持续不收敛则 `degraded_max_turns`+exit 2+hypotheses 全丢（这是「不收敛降级失败模式」的可测验收）；(b)「真实模型是否遵守『补查与引用分轮』指令」标为 **live 烟测观察项、非 CI 验收**（类比 D-10 对真实 cache hit rate 的诚实降级，prompt 指令有效性不可在 CI 用 authored cassette 自证）
- [x] 4.2 `DiagnosticianAgent`（包 AgentLoop + 渲染 diagnostician.md，工具总览按 spec.name 升序 byte-stable；模板缺失构造期 raise ConfigError）；`run(intent, findings, *, observer=None)`：findings 进**首条 user message 含序号标签**、**禁止**进 system（**机制**：`AgentLoop.run` 只收单个 `intent: str` 并自建 messages，故把 intent + 带标签 findings 块拼成该单一字符串传入，**不改 `AgentLoop` 签名**）；observer 透传；backend 只注入 loop（ADR-008）；budget 复用 settings.agent 配置值、计数独立；验收：findings 不入 system 的单测
- [x] 4.3 harvest（编排层）：遍历 `diagnostician_loop.tool_invocations` 的成功 correlate_findings 调用，从 **`inv.input`** 读 description/confidence/suggested_actions/supporting_findings(标签)，用编排层持有的 `FindingStore.resolve(label)→真 id` 解析后组装 `list[RootCauseHypothesis]`（**resolve 在 harvest 发生，非 handler；handler 仅校验命中**）；验收：两次调用→两条假设、且断言 supporting_findings 是 harvest 时 resolve 出的真 id（非标签）的单测
- [x] 4.4 reconcile：实现 D-5 全集映射（Planner=ok 时覆盖 Diagnostician **全部 7 个** `_TerminalStatus` 值：ok→ok / 四个 degraded_*→同名 / empty_response→同名 / **failed_api_unavailable→degraded_no_planner**；Planner 五个降级值→跳过诊断取 Planner 值、diagnostician_loop=None；Planner failed_api_unavailable→不产结果；禁止产 partial）；验收：(a) **参数化覆盖 Planner=ok × 7 个 Diagnostician 值 + Planner 各降级值**的穷举单测；(b) **Planner 降级时断言 `DiagnosticianAgent.run` 零调用**(mock not-called，证明是「不发起诊断」而非「发起后丢弃」)
- [x] 4.5 prompt cache **可测验收**（拆为 CI 真能测的）：(a) 两个不同 intent/findings 各跑一次，断言 system block 字节一致；(b) 断言 `DiagnosticianAgent` 把 system 包成**单元素 text-block list**（`[{"type":"text","text":...}]`，对照 planner.py 注释「bare str 会让 `_inject_cache_control` 走 skip 路径」——这才是诊断师侧要守的 invariant，而非重测 loop 既有注入逻辑）；(c) `prompt_caching=False` 时断言请求无 `cache_control`。**真实 cache hit rate（cache_read 计数）依赖真打 API，仅在 live 烟测验证，FakeBackend/PlaybackBackend 不断言真计数**

## 5. CLI 编排与渲染

- [x] 5.1 `cli/_intent.py`：串接 Planner→id 盖章(FindingStore seed)→DiagnosticianAgent，两段进度均走 CLI observer→stderr；产出 `DiagnosticianResult`；Planner=failed_api_unavailable 时走 no-result 路径（stderr 降级行 + stdout 空 + exit 2，不产 DiagnosticianResult）；backend 不进任一 ToolContext；registry 单实例共享；验收：stderr 两段进度 + stdout 仅报告的 CliRunner 测试 + no-result 路径测试
- [x] 5.2 **新增** `render_diagnostician_result`（不改既有 `render_planner_result`，后者 demo 仍用）：md（诊断 narrative**容忍空、空时不渲染空标题** + `## Findings`(顶层 canonical) + `## 根因假设`(空→`_暂无根因假设_`) + 遥测行）/ json（DiagnosticianResult 序列化）；验收：md snapshot（`.rstrip("\n")` 容忍尾换行）、空假设占位 snapshot、空 narrative snapshot、**empty_response(findings 非空) vs ok-无假设(findings 非空) 两条相邻路径 stdout 除遥测 status 行外一致的区分 snapshot**
- [x] 5.3 `cli/inspect.py`：`--intent` 退出码改由**新增** `_compute_diag_exit_code(DiagnosticianResult)` 映射（0/1/2/3）+ Planner failed_api_unavailable 无结果→2；区分 end_turn-有文本-无假设(ok→按 severity) vs empty_response(→2)；保留 `--persist` 对 `--intent` 拒绝(exit 3)；**不改既有 `_compute_intent_exit_code(PlannerResult)`（demo run 仍用）**；`--inspector` 路径零改动；验收：六个退出码场景 CliRunner 测试（含 empty_response→2、reconcile degraded_no_planner→2、no-result→2）+ 断言 demo run 退出码/渲染不回归
- [x] 5.4 **CLI --json schema 稳定性**：断言 `--intent --format json` 可被 `DiagnosticianResult.model_validate_json` 往返解析、字段集稳定；断言下游读**顶层 findings**（非 planner_result.findings）—— 顶层 `findings[*].id` 全非 None，`planner_result.findings[*].id` 全为 None（canonical vs 调试原件可观测区分）；验收：往返 + id 区分 snapshot 测试

## 6. cassette 与端到端验收

- [x] 6.1 录制诊断路径 cassette —— **沿用既有 `tests/incidents/_generate.py` / `tests/cli/test_inspect_intent.py::_record_cassette` 机制：`RecordingBackend` 包**脚本化 `FakeBackend`(authored responses)**，零 Anthropic key、确定性、可重现**（**禁止**真 key —— 既有 8 份 incident cassette 全 FakeBackend 录、proposal Demo Path 也承诺「无付费 API」）。authored 响应序列须确定性地包含：Planner 调 ≥1 inspector → Diagnostician 调 `correlate_findings`（supporting_findings 用作者写死的标签）+ 至少一次 `request_more_inspection`（验证 FindingStore 快照纳入新 finding）。target 用 `--intent` 实际装配的真 target（既有 harness 用真 local-host + 简单 inspector 如 hello.echo；**不引入 demo 的 ReplayTarget**）。**补查 inspector 必须 clock-free（无 `collect.sampling_window`，如 hello.echo）**：`--intent` 路径传 `clock=None`→真 UTC，带 sampling_window 的 inspector 命令会随时间漂移 → finding message/id 不稳定致快照 flake；clock-free 是本路径可重现的前提（与 D-9(c)「frozen-clock 仅 replay 装配传入」边界一致）。**因标签由作者写死，无需「人工核对真实模型抄对标签」**；验收：cassette_key 归一化命中、record→replay 复跑稳定
- [x] 6.2 端到端 replay 测试 `tests/cli/test_inspect_intent_diagnostician.py`（**沿用 `tests/cli/test_inspect_intent.py` pattern：monkeypatch `cli._intent.create_backend` 注入回放 6.1 cassette 的 backend**，真 local-host target，**不经 `demo run`**）：CliRunner 跑 `inspect --intent`，stdout 含根因假设章节（每条带证据链接，含至少一条引用 `request_more_inspection` 新 finding 以验证 FindingStore 快照）；验收：CliRunner + cassette，无真实 API、可重现
- [x] 6.3 **Anthropic degraded 验收**：(a) Planner ok + Diagnostician rate-limit-exhausted → `status=degraded_rate_limited`、exit 2、stdout 输出 findings + 空假设占位 +（**可能为空的**）narrative、CLI 未重试；(b) Planner ok + Diagnostician 调工具前不可达 → `status=degraded_no_planner`、exit 2、Planner findings 保留；(c) Planner `failed_api_unavailable` → 不产 DiagnosticianResult → no-result 路径 exit 2、stdout 空
- [x] 6.4 **密钥脱敏**：断言 hypotheses 文本（含 description/suggested_actions）/ DiagnosticianResult json / stderr 进度均不含环境变量值或凭据（诊断师消费的 findings 已脱敏，确认无新泄露路径）
- [x] 6.5 `mypy --strict` 全绿、`ruff` 全绿；更新 `TODO.md` M3.1 勾选（并注明 Report.hypotheses 持久化部分由后续提案交付）；`openspec-cn validate add-diagnostician-agent` 通过
