## 1. 录制 lane 基础设施(复用基底模式)

- [x] 1.1 扩 `tests/inspectors/compose/`:加单实例 postgres(固定 digest)+ 单实例 nginx(固定 digest,**镜像须配 stub_status location** 供 `curl` 探活;**不**用 systemctl 路径——compose 容器无 systemd,见 design D-5)+ 一个低 `max_connections` postgres 实例用于 semantic-abnormal;docker images/networks 用本机 docker daemon(不入 compose)
- [x] 1.2 复用基底录制器脱敏 + 就绪轮询模板(redis `PING` / postgres `pg_isready` / nginx `curl -fsS`),写进各 `_record_*.py`
- [x] 1.3 约定 docker 录制入口:构建固定大小本地镜像(images.disk_usage)、创建固定未挂载 user-defined network(networks),录后清理

## 2. redis.persistence(已证 client)

- [x] 2.1 写 `src/hostlens/inspectors/builtin/redis/persistence.yaml`:secret `HOSTLENS_REDIS_PASSWORD`→`REDISCLI_AUTH`、`redis-cli INFO persistence`、collector 抽 `aof_enabled`/`rdb_changes_since_last_save`/`rdb_last_save_time` 聚合标量(`aof_enabled` 入 output_schema)、finding **以 `aof_enabled == 0` 为前提门** + `rdb_changes_since_last_save >= warn_changes`(防 AOF 开启假阳性,见 design D-8;`warn_changes` 默认值随真实量纲定)、声明 `collect.timeout_seconds` 且 `redis-cli -t` < 该值、fail-loud + 数值校验
- [x] 2.2 录 fixture:healthy（少量未保存变更）/ conn_refused（连不存在端口）；无鉴权实例导出空串 secret
- [x] 2.3 录 semantic-abnormal fixture：**AOF 关闭实例** + 固定大量写入后 `rdb_changes_since_last_save` 在**默认阈值**超阈触发 finding（即时快照，不依赖 failed-save）。**阈值绑定**:`warn_changes` 默认值须落在 `healthy.rdb_changes < warn_changes ≤ abnormal.rdb_changes` 区间、由**真实异常态写入量**确定(非为凑触发压低阈值),录制与定值同一 PR 闭环,snapshot 对 healthy(无 finding)/abnormal(默认阈值出 finding)双向锁
- [x] 2.4 `tests/inspectors/test_redis_persistence.py`：snapshot 断言 healthy 无 finding、semantic-abnormal(**AOF 关闭** + 默认阈值)出预期 severity+message、**AOF 开启 + `rdb_changes_since_last_save` ≥ `warn_changes`(同 abnormal 量纲,仅 `aof_enabled` 不同)→ 不出 finding**(证「不出 finding」纯由 AOF 门贡献,非计数没超阈的 vacuous 通过)、conn_refused→exception、缺 redis-cli→requires_unmet、缺 secret env→requires_unmet

## 3. postgres.connection_usage(新 client psql + PGPASSWORD remap)

- [x] 3.1 新建 `src/hostlens/inspectors/builtin/postgres/connection_usage.yaml`（见 proposal 完整示例：secret `HOSTLENS_POSTGRES_PASSWORD`→`PGPASSWORD`、`PGCONNECT_TIMEOUT=5`、`psql -tA -F'|'` 一次往返取 `pg_stat_activity` 计数 + `max_connections`、command-sub 取 raw 再单独 awk、fail-loud + 数值校验、host/user/dbname 经 `| sh`+pattern、port int）
- [x] 3.2 实测 `PGPASSWORD` env 鉴权在录制环境生效、`pg_stat_activity` 计数为全局后端数（非 per-db 切片）
- [x] 3.3 录 fixture：healthy / access_denied（录制前提：`HOSTLENS_POSTGRES_PASSWORD` 设**错值**而非不设）/ conn_refused
- [x] 3.4 录 finding-trigger（低 warn 触发 warning）+ semantic-abnormal（低 `max_connections` + 保持固定连接数造真实高连接率，**默认阈值**触发 critical）
- [x] 3.5 `tests/inspectors/test_postgres_connection_usage.py`：snapshot 断言 healthy 无 finding、finding-trigger 出 warning、semantic-abnormal 默认阈值出 critical(**同时断言 severity + message 语义**,对齐 spec R5.1)、access_denied/conn_refused→exception、缺 psql→requires_unmet、缺 secret env→requires_unmet。**注**:finding-trigger(低阈值)仅验 finding wiring,**验收充分性以 semantic-abnormal 默认阈值断言为准**(契约硬条款:健康态+低阈值不满足验收)

## 4. docker.images.disk_usage + docker.networks(socket 路径,无 secret)

- [x] 4.1 写 `src/hostlens/inspectors/builtin/docker/images_disk_usage.yaml`：`docker system df --format '{% raw %}{{json .}}{% endraw %}'`(`{% raw %}` 包裹 Go-template 大括号避 Jinja 冲突,见 design D-7)+ **`jq`** 选 `Type=="Images"` 行、**直接解析 docker 自报 `Reclaimable` 字段的百分比 `(NN%)`** 得 `reclaimable_pct`(数值;**`// 0` 只兜「Images 行存在但 `Reclaimable` 无 `(NN%)`」**——零可回收/某些 docker 版本形如 `"0B"`,此时 0% 是真值;**但「`jq` 选不到 `Type=="Images"` 行」属采集异常,须 fail-loud → exception,不得被 `// 0` 掩盖成 0%**);`size`/`reclaimable` 作信息性字符串保留,**不**声明 `_bytes` 数值字段(规避 "6.952GB" 字符串→字节转换)、finding `reclaimable_pct >= warn_reclaimable_pct`(默认 80)、**`timeout <N> docker …`** 包裹(N < `collect.timeout_seconds`,`timeout` 入 `requires_binaries`——既有 docker 探针不含 `timeout`,此为新增系统二进制前提;manifest `description`/`tags` **须文档式声明** coreutils `timeout` 平台依赖,见作者契约)、**避 pipe-exit 陷阱**:docker 输出先 command-substitution 捕获 + `|| exit 1` 单独 gate、再另起一行喂 jq(jq 也单独 gate),**禁** `docker … | jq` 裸管道(POSIX sh 只传 jq 退出码、docker 失败被掩盖,复刻 `containers_restart_loop` 做法)、fail-loud（daemon 不可达→exception）、**不**硬编码 `requires_files: [/var/run/docker.sock]`（`DOCKER_HOST` 可能他指）
- [x] 4.2 写 `src/hostlens/inspectors/builtin/docker/networks.yaml`：`docker network ls/inspect` + **`jq`**(不用 `--format '{{}}'`)关联出**未被任何容器使用的非内置 user-defined network**(冻结语义见 design)、输出 `dangling_networks`(total 计数)+ `results`(**top-N 截断**,参数 `max_results` 默认 50,列表型故顶层用 `results`)、finding `dangling_networks >= warn_count`(默认 `warn_count: 1`)、**禁**把网络总数或内置网络(bridge/host/none)计入、`timeout <N> docker …` 包裹(`timeout` 入 `requires_binaries` + description/tags 声明 coreutils 依赖)、**避 pipe-exit 陷阱**(逐条 docker 调用 command-sub + `|| exit 1` gate,再喂 jq,禁裸 `docker … | jq` 管道)、fail-loud
- [x] 4.3 录 fixture：两者各 healthy + semantic-abnormal（images：固定大尺寸/可回收镜像使 `reclaimable_pct` 超默认 80；networks：创建固定个未挂载 user-defined network 触发）+ daemon 不可达→exception(录制手法:`DOCKER_HOST=tcp://127.0.0.1:1` 指死端点使 `docker` 非零退出,与 conn_refused 同类)
- [x] 4.4 `tests/inspectors/test_docker_images_disk_usage.py` + `test_docker_networks.py`：snapshot 断言默认阈值检出(**同时断言 severity + message 语义**,对齐 spec R5.1)+ daemon 不可达→exception + 缺 docker/jq/timeout→requires_unmet

## 5. nginx.health + nginx.config_test(新建 builtin/nginx/)

- [x] 5.1 新建 `src/hostlens/inspectors/builtin/nginx/health.yaml`：**no-finding** 失败三态、采集走 **`curl -fsS --max-time <M> http://{{ host | sh }}:{{ port }}{{ stub_status_path | sh }}`**(`port` 后**无字面 `/`**——`stub_status_path` 自带前导 `/`,避免 `//path` 双斜杠 404;`requires_binaries: [curl]`;**不**用 `systemctl is-active`——见 design D-5)、**注入安全**:`host` pattern `^[a-zA-Z0-9._-]+$`、`port` int、`stub_status_path` 收紧 pattern `^/[A-Za-z0-9/_-]+$`(自带前导 `/`、禁 `../`/scheme/query)且经 `| sh`、**collector 抽取 + 声明 schema**:`awk`/`sed` 从 stub_status 文本抽 `active_connections` 整数、`printf` 出 `{"healthy":true,"active_connections":N}`、`parse.format: json`、`output_schema` 声明 `healthy`(bool)+`active_connections`(int)、up→ok、down(nginx 停/endpoint 不可达 curl 非零 / 抽不出字段)→exception(fail-loud,绝不伪造健康)、`findings: []`、`curl --max-time` < `collect.timeout_seconds`
- [x] 5.2 写 `src/hostlens/inspectors/builtin/nginx/config_test.yaml`：**finding-route + collector 按 `nginx -t` 退出码分流**(见 design D-5):`err=$(nginx -t 2>&1); rc=$?` 后——`rc==0` → `{config_valid:true,...}` + exit 0;`rc==1`(配置有误)→ `jq -n --arg d "$err" '{config_valid:false,detail:$d}'` + **collector 自身 exit 0**;**其余一切非 {0,1} 的 rc(126/127/>128 signal 及 2..125 等)→ collector exit 1 + 空 stdout → exception**(白名单 finding + 其余全 exception 兜底,不留 rc 区间空白,**禁**把 rc 2 误吞成 config_valid:false)。**JSON 必须用 `jq -n --arg` 转义**(禁 `printf '{"detail":"%s"}'`——stderr 含引号/反斜杠/换行会产非法 JSON;`jq` 入 `requires_binaries`)。`parse.format: json`、`output_schema` `config_valid`(bool)+`detail`(string)、finding `when: "config_valid == false"`(此 collector 与同批次「非零→exception」fail-loud 方向相反,manifest 注释须显式说明)、声明 `collect.timeout_seconds`
- [x] 5.3 录 fixture：health up（→ok）/ health down(nginx 停 → curl 非零 → exception)；config_test 合法配置（→ok 无 finding）/ 静态无效配置（→预期 finding,默认严重度,这是 config_test 的 semantic-abnormal fixture)
- [x] 5.4 `tests/inspectors/test_nginx_health.py`：snapshot 断言 up→ok **且 parse 出 stub_status 实际字段**(证 curl 真打到 stub_status 而非任意 200 路径,防 up 态假绿)、down→exception、缺 curl→requires_unmet（验证 no-finding inspector 豁免 semantic-abnormal 但证 up/down 两态)
- [x] 5.5 `tests/inspectors/test_nginx_config_test.py`：snapshot 断言合法配置无 finding、无效配置出预期 finding(**同时断言 severity + message 语义**,默认严重度,= semantic-abnormal)、缺 nginx→requires_unmet;**须含一份 stderr 带引号/反斜杠/换行的无效配置 fixture**(证 `jq -n --arg` 转义产合法 JSON、不退化成 exception)

## 6. 套件契约固化与跨 inspector 验收

- [x] 6.1 扩 `tests/inspectors/test_builtin_inspectors.py`：断言 6 个新 inspector 干净注册、registry `errors == []`
- [x] 6.2 扩 `tests/inspectors/test_builtin_capability_gate.py`：缺 client 二进制→requires_unmet、缺已声明 secret env→requires_unmet（redis/postgres）
- [x] 6.3 跨 inspector 静态断言守 `service-inspector-contract`：注入安全（host/user/dbname 经 `| sh`+pattern）、collector 无按 `target.type` 分叉、聚合型裸键不与 parameter 同名;**secret 不进 argv 用通用断言**(沿用基底 `test_manifest_command_has_no_argv_plaintext_password` 的形态):正确规则是——secret env-var **名**经 shell `${HOSTLENS_...}` 展开 remap 到 client 原生 env(如 `MYSQL_PWD="${HOSTLENS_MYSQL_PWD:-}"`)是**必须且正确**的(基底断言 `assert f"${{{secret}" in cmd`),**禁止**的只是:(a) secret 出现在 `{{ }}` Jinja 块(会渲染 secret **值**进命令)、(b) 命令行明文密码 flag(`-a <pwd>` / `-p<pwd>`)。client 原生 env 通道限于 suite-wide **允许集** `{REDISCLI_AUTH, PGPASSWORD, MYSQL_PWD}`(允许集非「必须出现」,wave-2a 实际只用前两者)。**实现注 1**:既有 `test_service_contract_crosscheck.py` 现为 per-client 硬编码(只枚举 redis/mysql),本任务**须改写成 manifest 枚举驱动 + 通用模式匹配**,纳入两个新 secret manifest(redis.persistence/postgres.connection_usage)。**实现注 2(关键)**:改写后的 `test_output_is_aggregate_scalar_object`(现 crosscheck:472,断言 output 无 `array` 字段)**必须按 output_schema 是否声明 array 字段(等价:顶层键是否 `results`/`items`/`records`)分流**——纯聚合标量输出禁 array,**含列表输出者(如 `docker.networks` 的 `results`,其 finding 是标量 `dangling_networks >= warn_count`、无 `for_each`)豁免该断言、改验「top-N 截断 + total 计数」**。**勿用「manifest 有无 `for_each`」作判据**:`for_each` 是 per-finding-rule 属性,`docker.networks` 输出 array 但 finding 无 `for_each`,按「有无 for_each」会把它误判为聚合型→`results` 假红(正是本注要防的)
- [x] 6.4 secret 不泄漏回归:**仅对声明了 `secrets`(非空)的 manifest 跑泄漏扫描**(redis.persistence/postgres.connection_usage;nginx/docker 四个无 secret 探针无泄漏面、不纳入,避免扫无关密码的 vacuous 断言);断言 fixture 的 stdout/stderr 不含明文 `HOSTLENS_REDIS_PASSWORD`/`HOSTLENS_POSTGRES_PASSWORD` 值;复用基底 `test_service_contract_crosscheck.py` 的反 vacuous harness——**须扩 `_RECORDED_SECRET_VALUES` 加入本批次每个 secret recorder 实际注入的密码常量**(无条件:postgres recorder 的 `HOSTLENS_POSTGRES_PASSWORD` 值、redis.persistence recorder 实际注入的密码值——**无论是否复用既有 redis 常量都须确认其在 `_RECORDED_SECRET_VALUES` 中**),否则该 manifest 的「secret 不泄漏」断言会扫一个其 fixture 里从不出现的值而 **vacuous 恒真**
- [x] 6.5 跨 inspector 断言**超时与输出纪律**(契约硬条款):每个新 manifest 声明 `collect.timeout_seconds`、client 连接超时(`redis-cli -t` / `PGCONNECT_TIMEOUT` / `curl --max-time` / docker 的 `timeout <N>` 包裹)**严格小于** `collect.timeout_seconds`、输出为聚合标量或截断 top-N + total(无高基数明细整体回吐;`docker.networks` 的 `results` 须 top-N)
- [x] 6.6 在**仓根 `TODO.md` §M6 覆盖矩阵**(单张扁平表,见 `TODO.md:445-449;CLAUDE.md §9 误引为 docs/TODO.md`)对 6 个 wave-2a inspector 的**已存在 cell** 打勾(nginx.health/nginx.config_test @ Nginx 行、postgres.connection_usage @ PostgreSQL 行、redis.persistence @ Redis 行、docker.images.disk_usage/docker.networks @ Docker 行)——**矩阵已存在,勿新建子表**;另**标注 Docker 行的 `docker.containers.unhealthy` cell 为「由 `docker.containers.restart_loop` 覆盖、不单列」**(D-4),避免该 cell 成 M6 退出条件核验时的永久空位

## 7. 收尾

- [x] 7.1 确认零对外契约变更：manifest schema / capability enum / parse format / Agent 工具数组未变；无新 Python 依赖
- [x] 7.2 `mypy --strict` + `ruff` + 全量 `pytest`（默认 ReplayTarget 回放，不消耗 API）全绿
- [x] 7.3 跑 Demo Path（proposal 所列命令）
- [x] 7.4 对本次变更跑对抗性 review（`/review-loop-codex`），triage + 修复到放行（含 secret/失败分类安全边界，属应 review 类）
- [x] 7.5 开 feature branch `feat/add-single-instance-service-inspectors` → commit → push → `\gh pr create --base main`，CI 绿后 squash-merge
- [x] 7.6 归档：`openspec-cn archive add-single-instance-service-inspectors`，delta 合入 `openspec/specs/service-inspector-suite/`
