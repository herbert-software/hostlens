## 1. 编排去重（抽 backend 可注入核心，D-1）

- [x] 1.1 从 `cli/_intent.py` 的 `run_intent_diagnosis` 抽出 `run_diagnosis_pipeline(backend, settings, context_factory, report_target_name, target_lookup_name, target_type, intent, *, tool_clock=None, observer=None, planner_result_sink=None) -> Report | None`（签名见 design D-1；`planner_result_sink: Callable[[PlannerResult], None] | None`）：主体（collector 装配 + Planner loop + Planner-phase snapshot seed + Diagnostician loop + 全量 snapshot 组装 + status 合并 + id 不变量 + `failed_api_unavailable` 早返 + no-result 判据）与现 `run_intent_diagnosis` **逐字一致**，仅把 backend / context_factory / tool_clock 改为入参注入，并把单个 `target` 拆成 `report_target_name`（喂 `Report.from_inspector_results`）与 `target_lookup_name`（喂 `register_diagnostician_tools(target_name=)`）；**核心不写死 `DEMO_TARGET_NAME` 断言**（那会破坏 `--intent` 的真实 target，D-1 不变量「lookup 名==registry 注册名」由 **demo 装配侧**断言、见 2.1，核心至多做「`target_lookup_name` 在所供 context 的 registry 中」的通用校验）；核心内 `register_default_tools(planner_registry, collector=collector, clock=tool_clock)`（**必须**透传 `clock`）；`planner_result_sink` 在 `planner.run()` 后调一次（`--intent` 传 None=no-op，供 demo 录制 harness 投快照，D-3.5 step3）。**对"逐字一致"的例外（D-7）**：seeding 步骤 `_seed_findings_from_snapshot` 在 **stamp（`compute_finding_id` 填 id）之后、`store.seed` 之前**对 Planner-phase findings 按**渲染投影超集键** `(inspector_name, inspector_version, severity, tuple(f.tags), len(f.evidence), message)` 排序（覆盖 `_render_findings_block` 实际渲染的每个 per-finding 字段、tags 用原序非 sorted、evidence 用 len；**不能**只用 `finding.id`——severity-agnostic 会 tie 回退 collector 非确定序）使 F1/F2 跨 run 确定；**不加 fail-loud 守护**（key tie ⇒ 渲染逐字相同 ∧ 同 id → 无害，单向蕴含，见 D-7）；键须随渲染字段同步（单向 superset）。验收：核心函数单测（注入 FakeBackend + 假 context_factory 跑通组装 Report；断言 report 名与 lookup 名可不同；断言多-inspector snapshot 经两种 append 顺序输入 seeding 后 F1/F2 分配一致；断言排序键覆盖 `_render_findings_block` 全部 per-finding 渲染字段；`sink=None` 时行为与现 `run_intent_diagnosis` 逐字等价、`sink` 非 None 时收到 `PlannerResult`）
- [x] 1.2 `run_intent_diagnosis` 退化为薄包装：`create_backend(settings)` + 用真实 `target_registry`/`inspector_registry` 建 `context_factory` + `report_target_name=target_lookup_name=target` + `tool_clock=None` + 调 `run_diagnosis_pipeline`；验收：现有 `tests/cli/test_inspect_intent*.py` + `test_intent_report_assembly.py` 全绿（行为等价回归守护）——**例外**：若某测试 pin 了 D-7 排序前的 F1/F2 具体分配，按新稳定顺序更新（之前非确定，pin 本就脆），其余零改动

## 2. demo 全链装配（Planner→Diagnostician→Report，D-1/D-2/D-3）

- [x] 2.1 扩展 **`src/hostlens/demo/assembly.py`**（`build_demo_planner` 在此、**非** cli/demo.py）：**新增** `build_demo_pipeline(scenario_key, *, exit_stack)` 返回 **4 元组 `(backend: PlaybackBackend, context_factory, replay_target, settings)`**（`build_demo_planner` 若仅 Planner-only 录制/旧测试用则保留，否则随 demo run 改造一并删；tasks 5.1 的 re-point 范围据此定）——`context_factory` 复用 assembly 现有闭包（`target_registry` 含注册为 `incident-host` 的 `ReplayTarget` + 内置 inspector registry + `_DemoSettings` 屏蔽 `HOSTLENS_*`）；`register_default_tools` 调用补 `collector=`（现仅传 `clock=`）；Planner 与 Diagnostician 两段共享**同一个** `PlaybackBackend` 实例（D-2，单 cassette 按 key 匹配）；`settings` 必须是装配内部建的那个 `_DemoSettings` 实例本身、caller 原样回传 pipeline（request key 的 `model` 取自 `settings.agent.primary_model`，录制期与回放期必须一致，避免 caller 另建 `_DemoSettings` 致 model 漂移）；验收：装配后断言 backend 为 `PlaybackBackend`、两段同实例、回传的 `settings` 是装配内部实例；断言 demo 路径**不调用 `create_backend`**（monkeypatch `create_backend` 抛异常验证未被调，把"不触 API"从"同实例"加固到"根本没走真实 backend 工厂"）
- [x] 2.2 `demo run`（`cli/demo.py._run_scenario`）改调 `run_diagnosis_pipeline(backend, settings, context_factory, report_target_name=f"demo:{key}", target_lookup_name="incident-host", target_type=replay_target.type, intent, tool_clock=_frozen_clock, observer=observer)`（`backend`/`context_factory`/`settings` 均取自 2.1 的 4 元组）；**demo 装配侧断言 `target_lookup_name == DEMO_TARGET_NAME`（="incident-host"）== ReplayTarget 注册名**（D-1 不变量在 demo 侧守，使未来补 request_more_inspection cassette 不 miss；核心层不写死此断言以免破坏 --intent，见 1.1），返回 `Report | None`；**`ExitStack` 必须包住整个 `run_diagnosis_pipeline` 调用**（两 loop + seed + 组装 + `misses` 断言）——因单个 `PlaybackBackend`/`ReplayTarget` 的 packaged-asset reader 要在两 loop 全程打开，reader 提前关闭会让第二 loop（Diagnostician）`messages_create` 读不到 cassette；跑后保留 `replay_target.misses == []` 漂移守卫（诊断段只用 correlate_findings 不触 target，misses 仍应为空）；验收：demo run 端到端产出 Report 的测试 + 断言诊断段 `messages_create` 仍能读到 cassette（reader 未提前关闭）

## 3. 8 套场景补 Diagnostician 回放资产（D-2/D-3）

- [x] 3.1 为 8 套场景（cpu_saturation / dependency_unreachable / disk_inode / error_burst / fd_exhaustion / memory_oom / systemd_failed / tls_expiry）各补 Diagnostician 阶段 record（模型基于 Planner findings 调 `correlate_findings` 产 ≥1 根因假设后 finalize，**不调** `request_more_inspection`，零新增 target 命令）。录制按 **D-3.5 机制**（沿用 authored-response 离线录制，**零真实 key**，禁手编 request record），改造三处：
  - **扩展 `tests/incidents/_harness.build_authored_responses(scenario)`**：现仅 2 个 Planner turn，补诊断段 turn 后 happy-path 须返回 **≥4 个有序 `MessageResponse`**（`planner_tool_use`+`planner_end_turn`+`diag_correlate_tool_use`+`diag_end_turn`，各标 `stop_reason`，建议具名 helper 拆两段）；诊断段 `correlate_findings` 的 `supporting_findings` 引用 **ordinal label（如 `["F1","F3"]`）禁用 finding id**（已核 `diagnostician_tools.py:108`），label 须对照该场景 **D-7 排序后**的 seeded findings 列表写（录制脚本打印列表辅助；写错 label → 录制期 FakeBackend 无自纠错 → 该 hypothesis 静默丢 → 录成 0 假设，由本任务 ≥1 hypothesis liveness 守护拦截，**非** id-consistency invariant——后者只覆盖已 harvest 的 hypothesis）；
  - **重写 `tests/incidents/_generate.py::_record_cassette_and_snapshot`**：现 `build_incident_planner`（`_harness.py`）只返回 `(PlannerAgent, ReplayTarget)`、不暴露 `context_factory`/`settings`——须先在 `_harness.py` 加一个 `build_incident_pipeline_inputs(recorder, *, fixture_path) -> (backend, context_factory, replay_target, settings)` 抽取 seam（镜像 demo 的 `build_demo_pipeline` 4 元组），再 `run_diagnosis_pipeline(backend=recorder, context_factory=<该 seam 的 context_factory>, tool_clock=_frozen_clock, ...)` 跑全链；`RecordingBackend` 捕两段 request、整文件覆写场景 `cassette.jsonl`（`never append`）；target 侧 `ReplayTarget`（D-3 诊断段不触 target）；**不需** composite/phase-switch backend；
  - **snapshot 保持 Planner-only（单遍跑，用 sink）**：`snapshots/<key>.md` 与 8 个 `tests/incidents/test_*.py` 不迁 Report 投影；录制 harness 经 `run_diagnosis_pipeline` 的 `planner_result_sink`（tasks 1.1 加）拿 `PlannerResult` 投快照，`_generate.py:173/178` 的 `assert result.findings`/`project_planner_result(result)` 改消费该 sink；**否决"额外跑一遍 Planner-only"**（FakeBackend 脚本耗尽 + `_ACTIVE_CASSETTE_PATHS` 撞键 raise，见 D-3.5 step3）；
  - 验收（钉死 byte 级，非"回放绿/key 相等"）：重录后该场景 cassette 的 **Planner record 行与 `git show HEAD:` 版本逐字节相同**（`git diff --exit-code` 守护；**先验 cpu_saturation 确认 git diff 真空再批量重录**）、诊断 record key 与所有 Planner record key 不相交（防撞键）、诊断段 record 的 response content **无 `request_more_inspection` tool_use**（守 D-3 fixture 零改动前提）、每套全链回放 `replay_target.misses == []` 且 Report 含 ≥1 hypothesis（**liveness 守护**：抓"录成 0 假设"，非评估假设质量）
- [x] 3.2 8 套场景全链离线回放确定性验证（`tests/incidents/` 或 demo 测试）：每套断言 backend=PlaybackBackend、产带根因假设的 Report、退出码符合预期、诊断段无 `CassetteMiss`；**CI 常驻断言「8 套 fixture 所有 command `exit_code==0`」**（遍历 fixture.json 断言，非人工"确认"——含 `dependency_unreachable`，其 "unreachable" 是应用级 finding 非 inspector 级 `target_unreachable`，故 inspector 回放 ok；这是 spec partial 场景"8 套 demo 永不触发 partial"的守护，未来某场景补非 0 exit 命令会立刻红）；**第二消费者零回归断言**：8 个 `tests/incidents/test_<key>.py` 复用同一份变大 cassette 仍走 Planner-only `assert_incident_snapshot`，`PlaybackBackend` 按 key 匹配不强制全量消费 → 诊断 record 不被查找、incident 快照零回归（不改这 8 测试 + snapshots）；验收：8 套确定性通过、无真实 API。**这条 e2e 是 D-3.6 的常驻守护**（未来改某场景 Planner 资产忘了重录诊断 record → seeded findings 漂移 → 诊断段 CassetteMiss → 本测试立刻红），必须在 CI 常驻

## 4. demo 渲染 / 退出码 / 持久化（D-4/D-5）

- [x] 4.1 `demo run` 渲染改 `render_intent_report(report, fmt)`（md=intent 风格含根因假设 / json=Report 脱敏可往返），**移除** `render_planner_result` 调用；退出码改 `_compute_intent_report_exit_code(report)`；exit 3 caller 边界（pre-flight / --output 不可写 / 未知场景）不变；no-result(None)→stderr 降级+exit2；验收：md/json 渲染 snapshot（含根因假设、`.rstrip("\n")` 容忍尾换行）、健康(exit0)/critical(exit1) 退出码、json 往返 Report。**partial→exit2 由对 `_compute_intent_report_exit_code(Report(meta.status=partial))` 的单测覆盖**（不经 8 套打包 demo e2e——它们 inspector 全 ok 永不产 partial，见 spec），不新增畸形场景；`demo run --help` / demo 文档注明"遥测 token 为回放快照值、非真实计费"（不改共享 `render_intent_report`）
- [x] 4.2 新增 `demo run --persist`（默认关）：仅显式传且产 Report 时调 `_persist_report`（复用 `--intent` 的 **Report 路径** seam `inspect.py:638` 的 `(orphaned or persist_failed) and exit_code in (0,1) → 2`，**不是** `--inspector` 路径 `:971` 的 `== 0` 变体——否则 critical(exit1)+orphan 不会升 2）；no-result 跳过落盘；demo Report `report_target_name=f"demo:{scenario}"` 来源标记；`--persist --help` 文案警示"写入真实报告库（`$XDG_DATA_HOME/hostlens/reports.db`）"；验收：`--persist` 入库 + `reports show <run_id>` 全局取回含 hypotheses、取回 target_name=`demo:<scenario>`（不要求出现在真实 target 的 `reports list` 行里——见 spec 相容性说明）、不传 --persist 不入库、两次 run `reports diff` 跑通且 finding 级 delta 为空（同场景确定性回放 → 无 added/resolved，证 diff 管线可离线消费、非证 demo 能造变化；验收两次 `--persist` 入库为**两条不同 run_id**，否则自比无意义——已核 `Report.from_inspector_results` 用 `report_id = uuid4()`（models.py:450，每次组装新随机 uuid、**非**冻结时钟/内容派生），故两次确定性回放仍得两条不同 run_id，前提成立）、落盘**抛异常** exit2（stderr `internal:`）与**降级 orphan** exit2（stderr `warning:`）两分支、以及 **critical finding(exit1)+orphan → 升 exit2**（验 `in (0,1)` 谓词非 `==0`）的 CliRunner 测试（orphan 分支可复现触发：monkeypatch `ReportStore.save` 走 orphan 回退或令 `_persist_report` 返 `orphaned=True`，而非靠真实权限分裂）；**所有 `--persist` 测试必须 `monkeypatch.setenv("XDG_DATA_HOME", tmp_path)` 隔离 store 路径并断言不写真实用户库**（`_persist_report` 用 `ReportStore()` 无路径注入、解析 `$XDG_DATA_HOME/hostlens/reports.db`）

## 5. 收尾

- [x] 5.1 更新 demo 相关既有测试到 Report 契约：`tests/demo/test_demo_cli.py`（含 `test_demo_run_md_and_json_same_exit_code` 的 json 断言从 `loop_result.terminal_status` 迁到 `Report.meta.status`、md finding 行格式改 Report 风格、退出码断言对象改 Report）+ `tests/demo/test_demo_replay.py`（snapshot 迁 Report 风格）；**re-point** 既有 exit2 测试到重构后的**两个分离 seam**（D-1 后 `cli/demo.py` 不再引用 `build_demo_planner`，monkeypatch 旧名变静默 no-op 假绿）：装配期 ValueError（坏 cassette）→ monkeypatch `build_demo_pipeline` / `PlaybackBackend` 构造；运行期 `CassetteMiss`（现发生在 `run_diagnosis_pipeline` 内**第二个 loop**）→ 让 cassette 真缺一条 record 触发，或 monkeypatch `run_diagnosis_pipeline`（注入点与装配期**不再同一 seam**）；确认 `--inspector` + `--intent` 全套零回归
- [x] 5.2 把 **D-3.6 contributor 约定**写进贡献者文档（`CONTRIBUTING` / docs 或 demo scenarios README）：(1)「改某场景 Planner authored 响应/fixture 时，必须用扩展后的 `build_authored_responses` 整体重录该场景 cassette（Planner+诊断段一起）」；(2)「改 **D-7 排序键或 `_render_findings_block` 渲染字段**时，sort 顺序可能重排 → authored `[F#]` label 错位，必须对照新排序重写各场景 `supporting_findings` label 并整体重录」；8 套全链 e2e 回放（≥1 hypothesis liveness + misses==[]）是这两条耦合的常驻守护
- [x] 5.3 `mypy --strict src/` 全绿、`ruff check src/ tests/` 全绿、`openspec-cn validate wire-demo-to-report` 通过、全量 `pytest` 无回归；更新 `TODO.md`（M3 §「后续：demo wiring」标完成，hypothesis-level diff 仍列后续）
