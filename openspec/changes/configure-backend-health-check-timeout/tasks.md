## 1. 配置 schema（core-services）

- [x] 1.1 在 `src/hostlens/core/config.py` 的 `AgentSettings` 加字段 `health_check_timeout_seconds: float = Field(default=10.0, ge=1, le=120)`，紧邻 `health_check_model`；注释只写「为什么」（doctor health_check 探测的硬超时；默认 10 给慢 backend 余量、上界 120 保证 doctor 有界）。`AgentSettings` 类 docstring 现有「numeric bounds 防 token_budget 打字错」的笼统说明只举 token 字段为例——本字段的 bound 用途不同（响应性，非配额），若该 docstring 会因新字段读起来不准，补一句 timeout bound 的用途即可（别让它暗示所有 bound 都是 token 配额防线）。验收：`mypy --strict src/hostlens/core/config.py` 过。
- [x] 1.2 单测：`AgentSettings().health_check_timeout_seconds == 10.0`（缺省）；`HOSTLENS_AGENT__HEALTH_CHECK_TIMEOUT_SECONDS=40` 经 `load_settings()` 得 `40.0`；越界 `0` / `200` / 负数 + 非数字 `"abc"` 经 `load_settings()` raise `ConfigError`（对应 core-services spec 三个新场景）。**断言绑定到约束本身的完整子串、不要裸断单个数字字符**（Pydantic v2 实吐 `"Input should be greater/less than or equal to ..."` / `"valid number"`，非字面 `"must be in range"`）：消息恒含字段名 `"health_check_timeout_seconds"`；下越界 `0`/负数 `assert "greater than or equal to 1" in msg`；上越界 `200` `assert "less than or equal to 120" in msg`；非数字 `"abc"` `assert "valid number" in msg`。**关键——禁止 `assert "1" in msg`（vacuous）**：已实测 `_format_validation_error` 头部恒为 `"1 configuration error:"`，故 `"1" in msg` 对**每个**输入恒真（哪怕 `ge` 写错成 `999` 也绿），是任务自己要防的假绿；必须断完整约束短语（已实测「greater than or equal to 1」仅下越界出现、「less than or equal to 120」仅上越界出现、「valid number」仅非数字出现，三者互不串台、非 vacuous）。**同样禁止任何 `not in` 断言**（`"120"` 字面含 `"1"`、头部又注入 `"1"`，「不含 1」不可满足）。参照 `tests/core/test_config_backend.py:97-131`（max_turns 范围惯例），但**不要照抄其 `assert "1" in msg`**——那条同样 vacuous，本任务用更强的完整短语断言。验收：`pytest tests -k health_check_timeout` 绿。

## 2. doctor 消费配置（llm-backend-protocol）

- [x] 2.1 在 `src/hostlens/cli/doctor.py` 的 `_check_backend` 里**把生效 timeout 提为一个局部变量**（如 `effective_timeout = settings.agent.health_check_timeout_seconds if settings.agent is not None else _BACKEND_HEALTH_CHECK_TIMEOUT_SECONDS`），让 `asyncio.wait_for(..., timeout=effective_timeout)`（:652）与超时错误文案 f-string（:658）**同读这一个变量**——**禁止** :658 仍插 fallback 常量，否则 `agent` 非空时文案会渲染 fallback 值（如 `10.0s`）而实际等了配置值（如 `40.0s`），违反 spec「`{N}` 为实际生效秒数」。`_BACKEND_HEALTH_CHECK_TIMEOUT_SECONDS` 降级为「`settings.agent is None` 回落默认」**字面 float 常量**（值 = 字段默认 10.0；按 design D-3 **不**走 `model_fields[...].default`——`FieldInfo.default: Any` 会泄漏 `Any`）。验收：`mypy --strict src/hostlens/cli/doctor.py` 过（无 `Any` 泄漏）。
- [x] 2.2 防漂移单测：断言回落默认值 == `AgentSettings().health_check_timeout_seconds`（10.0），防止有人改字段默认忘了回落常量。验收：`pytest tests -k doctor_health_timeout_default` 绿。
- [x] 2.3 更新两处会变假的注释（F2，CLAUDE.md §6「注释只写为什么」）：①`src/hostlens/cli/_doctor_schema.py:199` 的 `"5-second timeout"` 改为「configured health-check timeout」之类不钉死秒数的措辞；②`src/hostlens/cli/doctor.py:573-577` 的常量 docstring（现 `:576` 描述「5 s ceiling is generous for a Haiku ping」）改写为「`settings.agent is None` 时的回落默认」角色。验收（**scope 到 backend-health 两串，勿碰 Non-Goal target probe**）：① 旧串删除——`grep -n "5-second" src/hostlens/cli/_doctor_schema.py` 改后 0 行 + `grep -n "Haiku ping" src/hostlens/cli/doctor.py` 改后 0 行；② **正向锚**（防把旧串删成空/无关串而没写对新角色——**必须用当前文件里不存在的精确短语，不要裸词**：裸 `configured` 在 `_doctor_schema.py:82/173/228` 已存在，`grep -qi configured` 会 vacuous）：`_doctor_schema.py:197-199` 改成含精确短语 `configured health-check timeout`（照此 verbatim 写，含连字符），验收 `grep -qi "configured health-check timeout" src/hostlens/cli/_doctor_schema.py`（当前 0 命中 → 非 vacuous；`-i` 容忍大小写）；`doctor.py` 常量 docstring 改成含 `fallback` 语义，验收 `sed -n '572,582p' src/hostlens/cli/doctor.py | grep -qi "fallback"`（窗口放宽到覆盖整段 docstring 防行号移位假红，当前该段无 `fallback` → 非 vacuous）。**不要**裸 `grep "5-second"`/`"5 s"` 扫 `doctor.py`——`:249/254/274` 是 target-exec `echo` probe（proposal Non-Goal，必须留 5s、勿动）。

## 3. doctor 行为回归（不需真网络）

- [x] 3.1 用可注入延迟的 `BackendDiagnostics` 桩（`health_check()` `await asyncio.sleep(...)` 后返回 `is_healthy=True`；复用 `tests/cli/test_doctor_backend.py:147-168` 的 monkeypatch 脚手架）。**关键约束**：`health_check_timeout_seconds` 有 `ge=1` 下界，**不能经配置路径注入 <1 的值**，故用合法 timeout。两条都**走真实配置路径**（`settings.agent` 非 None、`health_check_timeout_seconds` 经 `Settings`/`load_settings` 注入，**不**用 model_construct/monkeypatch）：① 慢但健康——配 `timeout=1.0`（最小合法值）+ 桩 sleep `0.1`（<timeout）→ `health_check_is_healthy is True`（不误报），约 0.1s；② **超时触发（同时钉死 task 2.1 的 `:658` 不变量）**——配 `health_check_timeout_seconds=1.0` + 桩 sleep `1.2`（>timeout）→ `False` 且文案**精确** `== "health_check timeout after 1.0s"`，wall-clock 约 **1.0s**（`wait_for` 在 timeout=1.0 处即取消、**不**等 sleep 跑满 1.2s；**勿**对该用例写 `~1.2s` 之类 wall-clock 断言，只断错误文案）。**② 必须经配置路径且取 `1.0`（≠ 回落常量 `10.0`）**：这是唯一能证伪「`:658` 仍插 fallback 常量」回归的验收——若实现者漏改 `:658`，文案会渲染 `10.0s` ≠ `1.0s` 而测试红。**不要**用 model_construct/monkeypatch 替代 ②（那会让 timeout 实参与 `effective_timeout` 脱钩、放过 `:658` bug）；如另想跑亚秒**附加**测试，可 `AgentSettings.model_construct(health_check_timeout_seconds=0.05)` 绕 `ge=1`（须注释说明为测速刻意绕过 Field 校验），但它是 ② 之外的补充、不替代 ②。避开边界相等（sleep 与 timeout 留间距，`wait_for` 边界相等行为未定义）。spec 场景里的 7s/8s 仅示意关系。对应 llm-backend-protocol spec「doctor 用配置的超时包裹 health_check」「超时是信息性」两场景。
- [x] 3.2 断言 health_check 超时**不**翻转 exit code：超时桩 + 其余检查全 ok → `hostlens doctor --json` 的 `ready is True`、进程 exit 0（backend 健康不参与 `_is_ready`）。同时断言 `BackendHealthRow` 字段集不变——6 个固定字段（`type` / `api_key_set` / `api_key_fingerprint` / `health_check_is_healthy` / `health_check_latency_ms` / `health_check_error`）+ `extra="forbid"`（`_doctor_schema.py:192-202`），仅 `health_check_error` 文案内秒数变化；保 doctor JSON schema 稳定性。
- [x] 3.3 `settings.agent is None` 回落：无 agent 块 + 可探测 backend → doctor 用回落默认 10.0 包裹、**不** raise `AttributeError`。对应 spec「`settings.agent is None` 时回落默认超时」场景。

## 4. 文档同步（F-1：新旋钮可发现性）

- [x] 4.1 更新用户面文档（缺则新旋钮无法被发现）。用**自完整判据**而非脆弱文件清单，三件事：
  - **A 逐字段用户面配置文档（scope 内自完整）**：scope = `docs/ + .env.example`（用户照抄配置的来源）。凡该 scope 下逐字段列 sibling agent 旋钮的文档都补 `health_check_timeout_seconds`，保持各自语法形态——`.env.example`（:48-53 通用参数块）补 env 形 `# HOSTLENS_AGENT__HEALTH_CHECK_TIMEOUT_SECONDS=10`；`docs/ARCHITECTURE.md`（**:1114-1120 配置 schema 的 agent YAML 块**）与 `docs/MIGRATION.md`（:14-29 agent 块）补 YAML 形 `health_check_timeout_seconds: 10`（缩进/无 `#` 前缀与同块既有字段如 `max_turns:` 一致——ARCHITECTURE 的 agent 块不含 `health_check_model:`，勿以它为锚）。集合由 `grep -rli "token_budget_input" docs/ .env.example` 定义（**必须 `-i`**：`.env.example` 用大写 env 形 `TOKEN_BUDGET_INPUT`，不带 `-i` 会漏掉它）；当前恰好这 3 文件。**显式排除仓根 `TODO.md:221`**（它也逐字段列 agent 字段但是已勾选 `- [x]` 的 M2 交付台账，补 M10.6 字段属历史失真——故 scope 不含仓根、只含 `docs/ .env.example`，「自完整」仅限此 scope、非全仓）。**scope 内自完整验收**（漏任一即报，`-i` 同时匹配大写 env 形 `HEALTH_CHECK_TIMEOUT_SECONDS` 与小写 YAML 形）：`for f in $(grep -rli "token_budget_input" docs/ .env.example); do grep -qi "health_check_timeout_seconds" "$f" || echo "MISSING: $f"; done` 输出**空**。
  - **B OPERABILITY SOT**：`docs/OPERABILITY.md:6` 自声明「健康检查」主题单一事实来源、proposal 涉及须引用。在其相关配额/健康检查表登记一行（默认 10s / 配置键 `agent.health_check_timeout_seconds` / 用途「防 doctor 因慢 backend 误报 timeout 或挂死」）。验收：`grep -ci "health_check_timeout_seconds" docs/OPERABILITY.md` ≥1。
  - **C doctor-unhealthy 排障处方**：`docs/ARCHITECTURE.md:1138` 提醒（现唯一归因 `health_check_model`）补第二致因——「健康但慢的 backend（DeepSeek/Qwen via OpenRouter）一次 ping >timeout 会被误报 timeout，调高 `agent.health_check_timeout_seconds`（默认 10）」。**机械 tripwire（必要、非充分、非 vacuous）**：① schema 块（:1114-1120，判据 A 职责）YAML 键形 `grep -qE "^[[:space:]]+health_check_timeout_seconds:" docs/ARCHITECTURE.md`（当前 0）；② 散文配置键形 `grep -q "agent\.health_check_timeout_seconds" docs/ARCHITECTURE.md`（当前 0）。**充分性靠 reviewer 人读确认**（不伪装成完备 grep）：散文那处 `agent.health_check_timeout_seconds` 必须落在 **:1138 doctor-unhealthy 提醒段**、构成「健康但慢→误报 timeout→调高该旋钮」的第二致因句。**诚实声明**：任何纯 grep 都可被「把串塞进无关行/尾注释」绕过（实测单行 `  health_check_timeout_seconds: 10  # 见 agent.health_check_timeout_seconds` 即同时命中①②却没改 :1138）——故散文落位**显式用人读验收**，tripwire 只保证「串存在」不保证「落对段」。（OpenRouter 节 `:1154` 可顺手同步一句，非硬验收。）

## 5. 密钥与文案脱敏复核

- [x] 5.1 复核改动不触及 `BackendHealth.error` 的 `redact_text` 链路（本提案不改 health_check 返回路径）；timeout 文案只含数字无密钥。验收：现有 `tests` 中 doctor 脱敏用例（`backend.api_key` 不入 JSON）仍绿，新增「timeout 文案不含任何 token 形态串」断言。

## 6. 收尾

- [x] 6.1 全量 `pytest` + `mypy --strict src` + `ruff` 绿。
- [x] 6.2 跑一次 Demo Path（proposal §Demo Path 三步：默认值 / env 覆盖 + 越界 ConfigError / 慢 backend 桩误报修复，桩走实现 `BackendDiagnostics` 的 backend monkeypatch 而非 FakeBackend），确认行为符合预期。注意超时文案是 float 形（如配 40 → `...after 40.0s`，非 `40s`），任何文案断言用 `.0` 形。
- [x] 6.3 `openspec-cn validate configure-backend-health-check-timeout --strict` 过；在 temp 副本实测 `archive` 的 rebuild 校验不报标题/格式错（参照 OpenSpec 归档易错点）。
